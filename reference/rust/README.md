# Rust scope

Not yet defined. Current org practice (see `ferromark`, `ferroni`, `ferrovia`):
plain toolchain defaults — no `rustfmt.toml`, no `clippy.toml` overrides.

A future `changes/` entry will define this scope. Until then, agents should
only apply the `common` scope to Rust repositories.
